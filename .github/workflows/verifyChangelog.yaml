name: Workflow caller - verify changelog updated 
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  verify-changelog:
    uses: BlueBuildr/.github/.github/workflows/verifyChangelog.yaml@main
