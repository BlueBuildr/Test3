name: Workflow caller - create release
on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: BlueBuildr/.github/.github/workflows/createRelease.yaml@main
