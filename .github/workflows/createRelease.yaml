name: CreateReleasse
on:
  pull_request:
    types: [closed]

jobs:
  verify-changelog:
    if: github.event.pull_request.merged == true
    uses: BlueBuildr/.github/.github/workflows/createRelease.yaml@main
